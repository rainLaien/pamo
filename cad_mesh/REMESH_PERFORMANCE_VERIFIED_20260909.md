# Remesh 性能实测（2026-09-09）

输入为 `debug/112/partition`，718,710 面；RTX 3060，目标边长 12，默认法向上限 5°，每批 75,000 个源面，翻边次数 0。保留采样预算、迭代上限、共享边和最终验证。

## 第四轮：只生成主 PLY（当前版本）

根据仅需要 `remesh_result.ply` 的实际用途，默认文件入口改为仅输出 `OUTPUT/remesh/remesh_result.ply`。最终 PLY 的 ASCII 格式、float64 坐标、颜色、共享索引和面属性均保持不变。首次完整运行已通过：总耗时由第三轮的 **144.46 秒降到 116.56 秒，减少 19.3%**；重网格化含读取和导出由 93.29 秒降到 80.67 秒。

缓存命中的完整运行也通过：**95.63 秒降到 80.95 秒，减少 15.4%**。两次均完整执行解析参数图、全部 13 个 CUDA 批次、曲面与全局几何/拓扑检查和最终主 PLY 导出。

| 相同测量范围 | 第三轮 | 本轮 |
| --- | ---: | ---: |
| 首次完整入口 | 144.46 秒 | 116.56 秒 |
| 缓存命中的完整入口 | 95.63 秒 | 80.95 秒 |
| 首次原生分区及缓存 | 49.81 秒 | 34.80 秒 |
| 重复运行：输入读取及校验 | 5.34 秒 | 3.41 秒 |
| 重复运行：重网格化，含读取和导出 | 93.67 秒 | 79.43 秒 |
| 重复运行：全部 CUDA 批次 | 57.26 秒 | 51.39 秒 |
| 重复运行：最终导出 | 6.15 秒 | 2.73 秒 |

原生分区计算本身没有再次修改，30.03 秒与第三轮入口中的 32.24 秒差异不能归因于本轮算法加速。交接文件由 233.26 MB 减少为 46.55 MB：二进制 PLY 28,748,502 字节，JSON 17,802,119 字节；缓存命中校验及路径准备为 0.38 秒。

两次输出目录均仅有 `remesh/remesh_result.ply`，574,708 个顶点、1,149,448 面，最长边 11.999794654872149，超长边 0。读回与第三轮输出对比，面索引、标签、曲面类型、角色数组完全一致；顶点最大分量差 9.095×10⁻¹³，最大位移 1.058×10⁻¹²。验证结束后独立计算质量，平均质量仍为 0.428130767276913，其他指标仅有浮点末位差异；该离线统计不计入上述运行时间。完整 PLY 并非逐字节一致。

大模型二进制/精简交接与原完整交接的顶点、面、标签、硬/平滑约束边和角点数组全部完全一致；8 个算法消费的 patch 字段、准备后的曲面域数组及合并策略也完全一致。

本轮实现的内容：

- 原生 `--remesh-handoff` 只输出标准 binary little-endian PLY 和精简 JSON。二进制顶点为 float64，面记录完整保留索引、标签、类型、角色与颜色；JSON 保留 remesh 使用的母面参数、成员、角色和几何约束。原生独立完整导出仍保留原 ASCII 与五份文件。新增导入、导出独立计时；本模型交接导出实测 0.862 秒。
- 分区缓存仅保存并校验两份交接文件。命中后直接只读加载；冷运行的临时交接文件保留到重网格子进程结束，默认不复制到结果目录。程序、输入、选项或相关运行库变化仍使缓存失效，损坏缓存重新生成。没有用缓存标记代替几何校验。
- 默认移除每批最终最近源面查询及其专用局部索引，保留优化过程中的源面传播、范围与分区归属核验。完整参考曲面的距离、法向、共享边和拓扑检查照常执行。
- 默认不计算纯报告用途的每批/全模型质量统计及全局抽样偏差。解析参数图仍使用完全相同的均值质量与接受阈值；不再为该条件计算无关的边长变异和最小角度。
- 单 PLY 导出在原有几何、拓扑、角点及边界来源检查后直接写出主文件，不构建辅助报告、另两份 PLY 或 STL。

`--full-output`（PowerShell 为 `-FullOutput`）可以恢复完整 remesh 诊断和辅助输出，完整文件入口也会保留两份内部交接文件。Python 库函数的默认值继续兼容既有使用：`collect_diagnostics=True`、`full_output=True`；默认 CLI 显式使用精简路径。`--trace-surface-validation` 仍保留原详细检查。

独立导出基准在相同必要验证下由 6.623 秒降到 2.634 秒，主 PLY 的 SHA-256 完全一致。真实 CUDA 第 2 批局部对照由 4.920 / 5.221 秒降到 4.361 / 4.503 秒；面索引、标签、11,418 次拆分、7,002 次折叠和 3 次松弛一致，顶点仅有约 10⁻¹² 的既有浮点归约差异。该基准只代表一个批次，不直接推算全部 CUDA 时间。

关联 Python/CUDA 测试共 147 项，145 项通过、2 项因旧可选夹具缺失跳过；另有 11 项新增二进制读取测试全部通过。原生测试以及 CPU 圆柱完整/精简交接对照通过。二进制读取在分配数组前核对实际负载长度，完整保留成员、拓扑、参数、约束和非法数据检查。

完整日志为 `debug/ply_only_cold.log` 和 `debug/ply_only_warm.log`，最新输出为 `debug/112_20260909_180622_338_6835c739/remesh/remesh_result.ply`。数组与文件对照见 `debug/ply_only_verification.json`。其他证据：`debug/ply_only_regressions.log`、`debug/ply_only_binary_regressions.log`、`debug/ply_only_native_tests.log`、`debug/optional_remesh_diagnostics_benchmark.json`、`debug/main_ply_export_benchmark.json`。

## 第三轮速度优化

依据补充的优化建议，本轮实现跨种子的 Analytic Seed Batch Scheduler，并减少最终曲面验证的重复最近点查询。默认入口首次运行完整通过，**全流程从 158.48 秒降到 144.46 秒，减少 8.8%**；本轮原生分区计算 32.24 秒，重网格化（含读取和导出）93.29 秒，其中全部 CUDA 批次 56.60 秒。两轮均关闭详细曲面 tracing，参数相同。

同一输入重复运行也完整通过，分区缓存校验耗时 0.59 秒，**全流程从 99.04 秒降到 95.63 秒，减少 3.4%**。以下为两个完整入口实测，均包含文件导出；未用局部内核加速比例推算总时间。

| 相同测量范围 | 第二轮 | 第三轮 |
| --- | ---: | ---: |
| 首次全流程，无分区缓存 | 158.48 秒 | 144.46 秒 |
| 重复全流程，命中分区缓存 | 99.04 秒 | 95.63 秒 |
| 首次重网格化，含读取和导出 | 96.12 秒 | 93.29 秒 |
| 重复重网格化，含读取和导出 | 97.01 秒 | 93.67 秒 |
| 首次全部 CUDA 批次 | 59.71 秒 | 56.60 秒 |
| 重复全部 CUDA 批次 | 60.62 秒 | 57.26 秒 |

两次均为 574,708 个顶点、1,149,448 面，最长边 11.999794654872149，超长边 0，全部几何/拓扑验证通过。读回完整 PLY 与第二轮重复运行对比：面索引、分区标签、曲面类型和角色数组完全一致；顶点最大坐标分量差为 9.095×10⁻¹³，最大位移约 1.061×10⁻¹²。平均三角形质量保持 0.428130767276913，其他质量指标仅有浮点末位差异。完整 PLY/STL 并非逐字节一致；本轮没有改变质量策略，也没有改善原先的质量问题。

### 原生分区：跨种子批处理

解析与残余搜索最多前瞻 8 个候选，以 256 个非线性种子为收集软上限；一个完整邻域可越过该上限。预取只计算拟合输入，不提前改变面归属、真实调度游标或候选提交顺序。实际评估时重新生成当前支持面，完整输入一致才复用求解结果。已完成的缓存受 4,096 个种子和 64 MiB 上限约束。

同一原生调用的局部对照从 **43.4043 秒降到 30.8742 / 30.6547 秒**，约减少 29%；CUDA 提交由 7,359 次降到 4,220 次。解析/残余预取批平均分别为 105.4 / 102.5 个种子；包含其他直接提交后，全局平均为 15.98 个种子，不能把预取均值当成全部调用均值。16 候选窗口仅测得 30.3218 秒，未用任务比例却从约 16.7% 增至 20.4%，因此保留 8。

8 窗口两次、16 窗口一次和事件计时一次，共四次完整原生运行的五个导出文件均与上一轮稳定基线 SHA-256 相同。原生既有测试及新增缓存键失效测试通过。最终程序 SHA-256 为 `018F7D5068771A9BFCE217BBA2BEFF0A8991D9083385AEA2069F538FF1465AB4`。

新增计时区分 CPU 打包、样本比较/复制、H2D 和回读等待，同时统计传输字节及上传缓存命中。该模型 H2D 为 81,859,080 字节，D2H 为 4,585,512 字节；样本缓存命中 1,658 次、未命中 2,562 次。CPU 比较和复制累计仅约 **0.012 秒**，因此未重写该缓存。

单独启用 `CADMESH_CUDA_PROFILE=1` 的验证记录 GPU 内核累计 13.589 秒，H2D 约 0.390 秒，回读等待约 13.875 秒。回读等待包含 GPU 执行与回传，不是纯 CPU 调度开销；这些阶段有重叠，不能相加。事件计时默认关闭，启用时也复用已有阻塞回读，不增加同步；该次总分区 32.5773 秒单独记录，不纳入上述速度对照。

### 最终曲面验证

新增 `valid_triangles_with_ties` 保留中心最近面和完整法向歧义判定，其余六个采样点改为阈值内存在性查询。成功路径返回与原接口相同的有效性和歧义标记；失败诊断、显式 tracing 和原 `return_details=True` API 仍计算精确距离与角度，CPU projector 继续使用原接口。

| 保存的实际批次 | 原详细检查中位耗时 | 新检查中位耗时 | 结果 |
| --- | ---: | ---: | --- |
| 317,601 面 | 0.3719 秒 | 0.0584 秒 | 有效性与歧义数组逐项一致 |
| 86,467 面 | 0.4510 秒 | 0.0680 秒 | 有效性与歧义数组逐项一致 |

局部检查加速约 6.4 倍不代表整条 remesh 加速 6.4 倍。84 项相关 Python/CUDA 回归全部通过；另有两路只读代码审查。松弛阶段增加廉价过滤的实验仅减少 0.101% 的曲面查询，未得到可靠性能证据，已精确撤回相关实现和试验测试。

补充建议中的关闭 tracing、解析感知拆分、松弛提前停止和圆柱非等距参数网格在当前代码中已存在，未将它们重复算作本轮成果。静态邻域缓存还需要覆盖面归属变化；异步传输、更多 proposal 复用及减少输出面数仍未实现，不能直接承诺其收益。

本轮日志：`debug/speed_round3_cold.log`、`debug/speed_round3_warm.log`、`debug/speed_round3_geometry_arrays.json`、`debug/speed_round3_regressions.log`、`debug/native_scheduler_verification.json`、`debug/final_validation_summary_benchmark.json`。首次完整输出为 `debug/112_20260909_174045_828_3cf306a7/remesh`，重复输出为 `debug/112_20260909_174324_580_e63f35f8/remesh`。以下保留之前各轮记录，时间须按相同测量范围比较。

## 第二轮速度优化

本轮按同一模型及相同参数，重新运行默认 `remsh.ps1` 两次，两次均完整通过。首次无分区缓存的全流程为 **158.48 秒**；同一输入再次运行命中分区缓存，全流程为 **99.04 秒**。这两个时间来自新增的 `[remesh-file] total wall time`，包含分区准备、输入读取、重网格化、验证和文件导出。

| 同口径阶段 | 上轮已验证版本 | 本轮首次运行 | 本轮重复运行 |
| --- | ---: | ---: | ---: |
| 原生模型分区计算 | 69.20 秒 | 43.75 秒 | 复用已校验分区 |
| 解析参数图 | 10.65 秒 | 10.47 秒 | 10.32 秒 |
| 全部 CUDA 批次 | 72.26 秒 | 59.71 秒 | 60.62 秒 |
| 重网格化，含读取和导出 | 109.21 秒 | 96.12 秒 | 97.01 秒 |
| 完整入口 | 上轮未计总墙钟时间 | 158.48 秒 | 99.04 秒 |

原生模型分区计算不包含原生导入、拓扑准备和原生文件导出，因此不能将该行与重网格化一行相加充当完整入口耗时。重复运行的分区缓存校验和读取为 0.60 秒；每次仍重新执行全部重网格化计算。

本轮保留的优化：

- 同一种子的独立模型拟合合并为 CUDA 批次，维持原候选评估和提交顺序。局部对照分区从 69.20 秒降到 43.40 秒，CUDA 提交从 24,545 次减少到 7,359 次；五个原生分区导出文件的 SHA-256 完全一致。
- CUDA 将校验缓存未命中的查询集中后执行，队列计数保留在 GPU。代表批次折叠中位时间从 1.787 秒降到 1.135 秒，五组输出数组及 5,340 次折叠完全一致。
- 非压缩折叠阶段复用双向均被拒绝的边；任一相关面改变后使结果失效。代表批次候选评估量从 2,555,672 次减少到 665,394 次；在上述 CUDA 优化上进一步从 1.201 秒降到 0.882 秒，输出数组仍完全一致。
- 局部细分使用整数边键匹配受保护边。代表批次从约 0.55 秒降到 0.30 秒，四组输出数组一致。解析投影仅保留当前批次涉及的模型，纯自由曲面批次省去无用计算。
- 原生分区缓存以实际输入、原生程序、分区参数、相关环境和运行库内容为键；完整校验文件后才复用，损坏时重新分区。可以用 `--no-partition-cache` / `-NoPartitionCache` 关闭。

两次完整输出均为 1,149,448 面，最长边 11.999794654872149，超长边 0，几何和拓扑检查通过。与上轮输出相比，三角形索引、分区标签、曲面类型和角色数组完全一致；顶点坐标最大分量差为 9.095×10⁻¹³，最大顶点位移约 1.05×10⁻¹²，质量指标仅有浮点末位差异。**完整 PLY/STL 文件并非逐字节一致**，这里的结果一致性指上述实际数组对照；本轮没有改善或降低网格质量。

本轮 81 项 Python/CUDA 关联测试全部通过，另有原生既有测试及新增预取缓存测试通过。无明确收益的距离见证查询实验已撤回。

验证文件：`debug/speed_round2_cold.log`、`debug/speed_round2_warm.log`、`debug/speed_round2_geometry_arrays.json`、`debug/speed_round2_regressions.log`、`debug/native_speed_verification.json`。第二轮重复输出位于 `debug/112_20260909_171607_278_c1112e9f/remesh`。以下章节保留第一轮实测记录，时间不能跨测量范围直接比较。

## 同输入局部对照

| 范围 | 修改前 | 修改后 | 验证 |
| --- | ---: | ---: | --- |
| 全部解析参数图 | 74.88 秒 | 11.15 秒 | 顶点、面、标签及 1,698 个接受分区完全一致 |
| 大批次 post-collapse | 4.110 秒 | 3.619 秒 | 五个输出数组逐项相等，折叠数相同 |
| 普通批次 post-collapse（预热后） | 2.46–2.62 秒 | 1.84–1.96 秒 | 五个输出数组逐项相等，折叠数相同 |
| 大批次 CPU 接口校验 | 约 1.12 秒 | 约 0.21 秒 | 校验规则保持一致 |
| 普通批次 CPU 接口校验 | 约 0.23 秒 | 约 0.04 秒 | 校验规则保持一致 |

解析图预检利用固定边的周向跨度证明部分参数图不可能满足内部边长限制，提前跳过 757 个失败尝试；相邻固定边形成三角形及周期缝别名仍按例外处理。未改变被接受参数图的生成流程。

CUDA 折叠缓存增至最多 2^22 槽（约 148 MiB），完整面索引和分区标签仍用于检查缓存命中。较大缓存减少冲突淘汰和重复曲面查询；大批次对照的 CUDA 峰值分配约 2.56 GiB。

CPU 接口检查改用整数边键去重；同一检查得到的边集合供质量统计复用。最终顶点压缩通过引用标记建立升序映射，避免重复排序全部面索引。最小角统计先归约最大余弦，再计算反余弦，测试网格的指标值保持一致。

顶点松弛的额外缓存实验没有稳定收益，未保留相关改动。

## 完整模型验证

修改后重新执行解析阶段、全部 13 个 CUDA 批次及全模型检查，记录的重建时间为 **102.84 秒**，其中解析阶段 14.59 秒、CUDA 批次 75.72 秒。该诊断运行还保存批次现场，时间不包含重新分区、输入读取或最终文件导出。

此前 98.42 秒的验证运行复用了已生成的解析结果，不能与上述 102.84 秒直接作总时间比较。可比的 CUDA 批次阶段从 85.67 秒降到 75.72 秒；post-collapse 合计从 25.52 秒降到 19.48 秒。

输出仍为 **1,149,448 面**，拆分数和折叠数与性能修改前相同。全局最长边 **11.999794655**，超长边 0；共享边坐标、角点、面关联和曲面检查通过，未产生重复面、退化面、裂缝或非流形边。

完整记录见 `debug/remesh_performance_full.log` 和 `debug/remesh_performance_verified/full_statistics.json`；解析图逐数组对照见 `debug/batch2_rejections/analytic_precheck_verification.json`。

## 重复运行入口

`remsh.ps1` 默认先使用 `debug/112`。该目录已经存在时，会选用带时间和随机后缀的新目录，并打印完整输出路径；可使用 `-OutputDirectory` 指定位置。显式目录已存在时仍由入口报告冲突，不覆盖已有结果。脚本内部路径根据脚本目录解析，不依赖调用者的当前工作目录。

已实际执行默认 `./cad_mesh/remsh.ps1`，从 `examples/111.stl` 重新完成原生分区、全部 13 个 CUDA 批次、全局验证及 PLY/STL 导出，进程退出码为 0。输出位于 `debug/112_20260909_162602_443_cc84c6df/remesh`，日志为 `debug/remsh_entrypoint_verified.log`。

该入口运行的重网格阶段（含读取和导出）为 **109.21 秒**：读取 5.41 秒、重建 97.55 秒、导出 6.25 秒；其中解析阶段 10.65 秒、CUDA 批次 72.26 秒。**109.21 秒不包含之前的原生分区**；原生模型分区本身另计 69.20 秒，且还有拓扑准备、文件暂存等开销，日志未记录整个脚本的总墙钟时间。

最终报告确认输出 1,149,448 面，最长边 11.999794654872149，超长边 0，全部全局验证通过。该完整入口结果与前述性能验证的面数和最长边一致。最近一轮关联测试共 101 项，99 项通过、2 项因缺少旧测试夹具跳过；另有新增拆分、约束和解析图预检回归测试通过。
